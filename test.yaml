---
gradientMode: "hue"
cellIdPreamble: "cell-"
cells: 
  ahoj:
    dataRef: "process_max_fds{job=\"flt-rest-01\"}"
    label:
      separator: "replace"
      units: "none"
    labelColor:
      threshold: *threshold_inflow

